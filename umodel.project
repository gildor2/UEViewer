# perl highlighting

R   = .
PRJ = umodel
!include common.project

sources(MAIN) = {
	Main.cpp
	Exporters/*.cpp
	Unreal/*.cpp
	Viewers/*.cpp
	MeshInstance/*.cpp
	Core/*.cpp
	UI/*.cpp
!if "$PLATFORM" eq "win32"
	UI/umodel.rc
!endif
}

target(executable, $PRJ, MAIN + NV_LIBS + UE3_LIBS + IOS_LIBS, MAIN)
