AS_VehiclesFull_M
  SpaceFighter_Human:
    13 sections + 12 static sections
  ! bugs, when showing smooth section only (for better visibility) and using LOD's SkinPoints
    array instead of Points array

DemoFemaleA/Skeleton_Female has texture DemoPlayerSkins.utx/DemoSkeleton with removed 1st 2 mipmaps
